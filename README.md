# ScreenSlate
Local screen/window recorder built with Electron + TypeScript. Pick a source, Start/Stop, save playable WebM to Downloads with timestamped filename. Optional mic audio. In-app log panel with timestamps.
